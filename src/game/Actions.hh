#pragma once

enum class Action
{

    // Movement
    MoveForward,
    MoveBackward,
    MoveLeft,
    MoveRight,


    // Actions
    Jump,
    Attack,

    // Menu
    Confirm,
    Cancel,

    MoveUp,
    MoveDown,

    // Camera
    LookLeft,
    LookRight,
    LookUp,
    LookDown,
    ZoomIn,
    ZoomOut,

    
    // Debug
    Debug,
    MoveCamera,
};